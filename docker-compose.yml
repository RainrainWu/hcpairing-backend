version: "3"

services:
    postgres:
        image: "postgres:13"
        env_file:
            - .env
        restart: always
        ports:
            - "5432:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data

volumes:
    pgdata: